# FATE-X GitHub Sync

This worktree intentionally ignores datasets, checkpoints, models and logs.

Placeholder:

```bash
git remote add fate git@github.com:<USER_OR_ORG>/FATE-X.git
git add .
git commit -m "Initialize FATE-X from ADAPT with dataset-safe structure"
git push -u fate fate-x
```
